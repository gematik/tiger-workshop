servers:
  demo:
    type: externalJar
    source:
      - local:src/demo-1.0.0.jar
    healthcheckUrl: http://localhost:8080/service/status
    # OPTIONAL
    healthcheckReturnCode: 200

lib:
  activateWorkflowUi: false